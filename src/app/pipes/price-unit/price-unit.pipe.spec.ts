import { PriceUnitPipe } from './price-unit.pipe';

describe('PriceUnitPipe', () => {
  it('create an instance', () => {
    const pipe = new PriceUnitPipe();
    expect(pipe).toBeTruthy();
  });
});
