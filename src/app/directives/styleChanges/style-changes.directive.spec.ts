import { StyleChangesDirective } from './style-changes.directive';

describe('StyleChangesDirective', () => {
  it('should create an instance', () => {
    const directive = new StyleChangesDirective();
    expect(directive).toBeTruthy();
  });
});
